\documentclass[a4paper]{scrartcl}
\usepackage{mystyle}

\newcommand{\slopesfigure}[1]{
  \begin{figure}
    \includegraphics[width=\textwidth]{{Slopes_FM_SPL-#1.00}.eps}
    \caption{Excitation patterns calculated using Terhardt's method for a
      frequency-modulated signal with $f_c = 1.5$ kHz, $f_m = 4$ Hz, $d_f = 700$
      Hz, SPL = #1 dB, $f_s = 44.1$ kHz, N~=~$2^{20} (1048576)$ samples}
  \end{figure}
}

\newcommand{\slopesfigures}[1]{
  \foreach \i in {#1} {
    \slopesfigure{\i}
  }
}

\begin{document}

\slopesfigures{40,50,60,70,80,90}

\end{document}
